export type Button = {
	buttonTarget: string;
	buttonValue: string;
	buttonText: string;
};

export type PaginatedButton = {
	buttonTarget: string;
	buttonValue: string;
	buttonText: string;
	buttonPage: number;
	buttonMax: number;
};
